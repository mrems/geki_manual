import { useTranslation } from 'react-i18next';

function Header() {
  // const { t } = useTranslation(); // Plus besoin de t

  return (
    <></> // Le Header est maintenant vide
  );
}

export default Header;
